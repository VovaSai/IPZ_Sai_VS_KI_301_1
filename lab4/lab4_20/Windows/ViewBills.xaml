<Window x:Class="lab2_20.Windows.ViewBills"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="Перегляд рахунків"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>


    <Border CornerRadius="20" Background="{StaticResource BackgroundGradient}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" />
        </Border.Effect>

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Панель заголовка -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="DragWindow">
                <DockPanel LastChildFill="False" Background="Transparent">
                    <TextBlock Text="Перегляд рахунків"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Medium"
                               VerticalAlignment="Center"
                               Margin="20,0,0,0"
                               DockPanel.Dock="Left" />

                    <Button DockPanel.Dock="Right"
                            Style="{StaticResource WindowControlButton}"
                            Click="CloseWindow"
                            Content="✕" />
                    <Button DockPanel.Dock="Right"
                            Style="{StaticResource WindowControlButton}"
                            Click="MaximizeWindow"
                            Content="☐" />
                    <Button DockPanel.Dock="Right"
                            Style="{StaticResource WindowControlButton}"
                            Click="MinimizeWindow"
                            Content="—" />
                </DockPanel>
            </Grid>

            <!-- Основний контент -->
            <Border Grid.Row="1"
                    Background="#1E2A3B"
                    CornerRadius="15"
                    Margin="0,10,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="-90" RenderingBias="Quality" ShadowDepth="2" />
                </Border.Effect>

                <ListView Grid.Row="1"
                          Margin="20"
                          ItemsSource="{Binding Bills}"
                          SizeChanged="DynamicListView">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Назва" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="White"
                                                   VerticalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Дія" Width="Auto">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,5">
                                            <Button Click="CheckInfoClick"
                                                    Content="📋 Інформація"
                                                    Style="{StaticResource OutlinedButtonStyle}"
                                                    Height="45"
                                                    Margin="0,0,10,0"
                                                    CommandParameter="{Binding}" />
                                            <Button Click="ChangeClick"
                                                    Content="✏️ Змінити"
                                                    Style="{StaticResource OutlinedButtonStyle}"
                                                    Height="45"
                                                    Margin="0,0,10,0"
                                                    CommandParameter="{Binding}" />
                                            <Button Click="DeleteClick"
                                                    Content="🗑️ Видалити"
                                                    Style="{StaticResource OutlinedButtonStyle}"
                                                    Height="45"
                                                    Margin="0,0,10,0"
                                                    CommandParameter="{Binding}" />
                                            <Button Click="PaidClick"
                                                    Content="💳 Оплатити"
                                                    Style="{StaticResource GlassButton}"
                                                    Height="45"
                                                    CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Grid>
    </Border>
</Window>