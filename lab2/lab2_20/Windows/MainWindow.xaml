<Window x:Class="lab2_20.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:lab2_20.Windows"
        mc:Ignorable="d"
        Title="Система управління рахунками" 
        Height="600" 
        Width="1000" 
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="20" Background="{StaticResource BackgroundGradient}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2"/>
        </Border.Effect>
        
        <Grid Margin="10">
            <!-- Верхня панель для керування вікном -->
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Панель заголовка -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="DragWindow">
                <!-- Кнопки керування вікном -->
                <DockPanel LastChildFill="False" Background="Transparent">
                    <!-- Заголовок вікна -->
                    <TextBlock Text="Система управління рахунками" 
                             Foreground="White" 
                             FontSize="16" 
                             FontWeight="Medium"
                             VerticalAlignment="Center"
                             Margin="20,0,0,0"
                             DockPanel.Dock="Left"/>
                    
                    <!-- Кнопки -->
                    <Button DockPanel.Dock="Right" 
                            Style="{StaticResource WindowControlButton}" 
                            Click="CloseWindow" 
                            Content="✕"/>
                    <Button DockPanel.Dock="Right" 
                            Style="{StaticResource WindowControlButton}" 
                            Click="MaximizeWindow" 
                            Content="☐"/>
                    <Button DockPanel.Dock="Right" 
                            Style="{StaticResource WindowControlButton}" 
                            Click="MinimizeWindow" 
                            Content="—"/>
                </DockPanel>
            </Grid>

            <!-- Основний контент -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Ліва панель з кнопками -->
                <Border Grid.Column="0" 
                        Background="#1a2332" 
                        CornerRadius="15" 
                        Margin="0,10,10,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Direction="-90" RenderingBias="Quality" ShadowDepth="2"/>
                    </Border.Effect>
                    
                    <StackPanel Margin="20" VerticalAlignment="Center">
                        <Button Click="CreateBillClick" Style="{StaticResource GlassButton}" Margin="0,5,0,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="+" FontSize="20" Margin="0,0,15,0"/>
                                <TextBlock Text="Створити рахунок" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Click="ViewBillsClick" Style="{StaticResource GlassButton}" Margin="0,5,0,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="Переглянути рахунки" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Click="ViewPaidBillsClick" Style="{StaticResource GlassButton}" Margin="0,5,0,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="Історія оплати" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Click="CloseClick" Style="{StaticResource DangerGlassButton}" Margin="0,20,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🚪" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="Вийти" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Права панель -->
                <Border Grid.Column="1" 
                        Background="#1E2A3B" 
                        CornerRadius="15" 
                        Margin="10,10,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Direction="-90" RenderingBias="Quality" ShadowDepth="2"/>
                    </Border.Effect>
                    
                    <Grid>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="Ласкаво просимо!" 
                                     Foreground="White" 
                                     FontSize="32" 
                                     FontWeight="Light"
                                     HorizontalAlignment="Center"/>
                            <TextBlock Text="Оберіть опцію з меню зліва для початку роботи" 
                                     Foreground="#8FA3BF" 
                                     FontSize="16" 
                                     Margin="0,10,0,0"
                                     HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>